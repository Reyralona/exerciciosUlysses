/*

8 . Descreva o funcionamento das seguintes funções:
    a) getchar()
    b) putchar()
    c) scanf()
    d) printf()

    a) A função getchar() faz parte do módulo "stdio.h" da biblioteca padrão. Essa função possibilita 
       uma entrada do usuário na entrada padrão, e lê um caractere inserido.
       Porém, a função não lê apenas um caractere, ela também lê o caractere "\n" que reside no final do buffer
       alocado pela função, que indica que foi o final da operação.
    b) A função putchar() faz parte do módulo "stdio.h" da biblioteca padrão. Essa função possibilita 
       enviar para a saída padrão um único caractere envolto por aspas simples.
    c) A função scanf() faz parte do módulo "stdio.h" da biblioteca padrão. Essa função permite que o usuário
       insira uma entrada mais completa e robusta, podendo incluir diferentes tipos e de tamanhos diferentes.
    d) A função printf() faz parte do módulo "stdio.h" da biblioteca padrão. Essa função permite que o usuário
       escreva o que deseja na tela, podendo-se utilizar de diferentes tipos e de tamanhos diferentes.
*/
